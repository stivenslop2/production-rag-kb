# landing

Placeholder for a future landing page (portfolio entry point).

When implemented, this feature will own the marketing/intro components rendered at `app/page.tsx` or `app/landing/page.tsx`.
